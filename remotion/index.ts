import { registerRoot } from "remotion";
import { RemotionRoot } from "./Root";

/**
 * Entry point for the Remotion project.
 * It registers the Root component which contains all video compositions.
 */
registerRoot(RemotionRoot);
